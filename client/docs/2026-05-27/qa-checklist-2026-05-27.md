# Fix Pass 회귀 검증 체크리스트 — 2026-05-27

> 직전 세션(2026-05-26 Fix Pass) 6건 처방의 회귀 검증.
> Windows 측에서 직접 실행해 주세요.

---

## 0. 사전 (1회만)

```powershell
# client 디렉토리에서
npm run typecheck    # tsc -b --noEmit
npm run build        # tsc -b && vite build
```

- [ ] typecheck 0 error
- [ ] build 성공 (`dist/` 생성)

> 이번 세션에서 `kakao.maps.CustomOverlay` 타입과 `LstmFullAnalysis` 의
> `ChartData<'line', ...>` 타입을 함께 보강했으므로, 이전 진단 시 떴던
> 3건의 컴파일 에러는 모두 해소되어야 합니다.

---

## 1. InfoTooltip Portal — stacking context 탈출 검증

`npm run dev` 후 `/recommendation` 진입.

### 1-A. 토글 버튼 vs 툴팁 z-order
- [ ] 좌측 패널(가중치) 열린 상태에서 "가중치" ⓘ에 호버 → 툴팁이 모든 UI 위에 뜸
- [ ] 좌측 패널 토글 버튼(`‹`)에 ⓘ 툴팁이 가려지지 않음
- [ ] 우측 패널 펼쳐진 상태에서 위와 동일 확인

### 1-B. 스크롤 컨테이너 클리핑 부재
- [ ] WeightSliders를 끝까지 스크롤 → "소득 분위" ⓘ 호버 → 툴팁 완전 표시 (잘리지 않음)
- [ ] 스크롤 영역 가장 위에서 "가중치" ⓘ 호버 → 툴팁 완전 표시
- [ ] 4개 가중치 라벨(통근/부담/안전/생활) ⓘ 호버 → 모두 잘림 없음

### 1-C. 좌표 갱신
- [ ] ⓘ에 호버한 채 마우스 휠로 패널 내부 스크롤 → 툴팁이 아이콘 따라 이동
- [ ] ⓘ에 호버한 채 브라우저 창 리사이즈 → 툴팁이 아이콘 따라 이동
- [ ] 호버 떼면 툴팁 즉시 사라짐

### 1-D. 키보드 접근성
- [ ] Tab 키로 ⓘ에 포커스 이동 → 툴팁 표시
- [ ] Shift+Tab으로 포커스 떼면 툴팁 사라짐

---

## 2. WeightSliders 급여 ↔ 분위 칩 3-way sync

### 2-A. ✕ 버튼이 store까지 리셋
- [ ] 월급여 `350` 입력 → "3분위" 칩 자동 활성 + hint "월 350만원 → 3분위" 표시
- [ ] ✕ 버튼 클릭 → 입력칸 비워짐 + hint 사라짐 + "미선택" 칩 활성 + "3분위(403만원) 기본값 적용 중" 메시지 표시
- [ ] Zustand devtools(또는 Network 탭의 `incomeMonthly` 파라미터)에서 `incomeQuintile === null` 확인

### 2-B. 칩 클릭이 입력 무효화
- [ ] 월급여 `200` 입력 → "2분위" 칩 활성 + hint 표시
- [ ] "5분위" 칩 클릭 → 입력칸 비워지고 hint 사라짐 + "5분위" 칩만 활성
- [ ] 화면에 3분위/2분위/5분위 같이 다중 분위 표기가 절대 보이지 않음

### 2-C. 입력 자체를 비웠을 때
- [ ] 월급여 `500` 입력 → "4분위" 활성
- [ ] 입력칸의 `500`을 직접 백스페이스로 모두 지움 → 입력 빈 상태 + hint 없음 + "미선택" 칩 활성으로 자동 복귀

### 2-D. 토글 동작 (활성 칩 재클릭)
- [ ] "3분위" 활성 상태에서 "3분위" 칩 다시 클릭 → "미선택"으로 토글되고 입력 잔여물 없음

---

## 3. ComplexCardList LH 카운트/필터 일치성

`/region-detail?legalDongCode=1156013000` (당산동) 진입.

### 3-A. 카운트 == 카드 수
- [ ] "전체" 탭 → 헤더 "매물 단지 3건" + 카드 3장
- [ ] "민간" 탭 → 헤더 "매물 단지 2건" + 카드 2장 + 탭 칩 "민간 2"
- [ ] "LH 국가" 탭 → 헤더 "매물 단지 1건" + 카드 1장 + 탭 칩 "LH 국가 1"

### 3-B. LH가 없는 지역
- [ ] `legalDongCode=1168010600` (대치동) 진입 → "LH 국가" 탭 누르면 "LH 국가매물이 없습니다." 빈 상태 표시 + 탭 칩 "LH 국가 0"
- [ ] "민간" 탭 카운트가 전체 단지 수와 일치 (대치=4, 방배=3, 목동=3, 한남=2)

### 3-C. 0 division 방어
- [ ] (mock에 0 단가 없으므로 정상 케이스만 확인) 카드의 "3년 변동 ±X%" 가 모든 단지에서 유한한 숫자로 표시 (`+Infinity%` 같은 값 없음)

---

## 4. MapPanel resize 리스너

### 4-A. 윈도우 리사이즈 대응
- [ ] `/recommendation` 진입 → 직장 선택 → 지도 정상 표시
- [ ] 브라우저 창 크기를 좁힘 ↔ 넓힘 → 지도 캔버스가 빈틈 없이 새 viewport에 맞춰짐 (relayout)
- [ ] devtools 열기/닫기 → 지도 정상 재배치

### 4-B. 패널 토글 (relayout 불필요 케이스)
- [ ] 좌/우 패널 토글 → 지도 캔버스 크기는 변하지 않음 (오버레이 아키텍처)
- [ ] 토글 애니메이션 300ms 부드럽게 동작

---

## 5. (참고) 알려진 환경 이슈

- bash sandbox 환경에서 `tsc -b --noEmit` 실행 시 stale mount 캐시 때문에 가짜 "Unterminated string literal" 에러가 100여 개 떨어질 수 있음. **Windows 측 실 환경에서 돌린 결과만 신뢰**.

---

_체크리스트 작성: 2026-05-27_
